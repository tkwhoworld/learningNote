package sort

import (
	"fmt"
)

func BubbleSort(arr [10]int){
	length := len(arr)
	fmt.Println("the length is:",length)
	for i:=0; i<length-1;i++ {
		fmt.Println("the length is:",i)
		for j:=0;j<length-1-i;j++ {
			temp := 
		}
	}
}